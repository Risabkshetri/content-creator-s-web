/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'images.unsplash.com', 'picsum.photos', 'example.com', 'i.imgur.com'],
        
      },
};

export default nextConfig;
